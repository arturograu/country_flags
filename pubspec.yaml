name: country_flags
description: A Flutter package to show a flag icon from a ISO country code.
version: 4.1.2
homepage: https://github.com/arturograu/country_flags
repository: https://github.com/arturograu/country_flags

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  collection: ^1.19.1
  flutter:
    sdk: flutter
  jovial_svg: ^1.1.25

dev_dependencies:
  flutter_test:
    sdk: flutter
  golden_toolkit: ^0.15.0
  very_good_analysis: ">=5.0.0+1 <11.0.0"

flutter:
  uses-material-design: true
  assets:
    - res/si/
